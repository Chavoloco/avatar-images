import React from 'react'
import AvatarTile from './AvatarTile'

describe('<AvatarTile />', () => {
  it('renders', () => {
    // see: https://on.cypress.io/mounting-react
    //cy.mount(<AvatarTile />)
  })
})